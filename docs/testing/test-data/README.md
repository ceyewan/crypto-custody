# 在线系统本地测试数据

这些 JSON 文件用于前端页面手动导入测试数据，不由在线服务端提供下载，也不放入前端静态资源目录，因此不会进入实际交付镜像。

导入顺序建议：

1. 在“案件管理”导入 `cases.json`
2. 在“账户管理”导入 `accounts.json`
3. 在“交易管理”导入 `transactions.json`

导入按钮只写入业务记录。交易 JSON 导入不会生成离线签名任务；需要签名流程时，请在交易列表中使用“导出任务”和“导入签名”。
