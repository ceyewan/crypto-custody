# middleware 目录

`middleware` 目录保存 Gin 中间件，用于统一处理日志、认证和角色权限控制。

## 当前职责

- 初始化全局中间件。
- 校验 JWT 登录态。
- 校验管理员权限。
- 校验警员或管理员权限。

## 维护建议

- 认证逻辑应只负责识别用户和写入上下文，不承载具体业务规则。
- 角色判断需要与 `model.User` 和路由权限保持一致。
- 新增中间件后，在 `route.Setup` 或对应路由组中显式挂载。
