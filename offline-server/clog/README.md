# clog 目录

`clog` 目录提供离线协作服务端的结构化日志能力，用于统一记录启动、HTTP、WebSocket、MPC manager 和存储层的运行状态。

## 文件说明

- `logger.go`：日志级别、字段封装、初始化配置和快捷记录方法。

## 维护建议

- 新增模块日志时优先复用本目录的字段方法，保持日志格式一致。
- 关键流程应记录会话、用户、地址或错误原因等可定位信息。
- 敏感数据不要写入日志，例如私钥分片、签名中间值和完整认证令牌。
