# model 目录

`model` 目录定义离线协作服务端的数据库模型。

## 文件说明

- `user.go`：用户账号、角色和认证相关字段。
- `session.go`：密钥生成或签名会话状态。
- `share.go`：密钥分片相关记录。
- `se.go`：安全芯片标识和 CPLC 信息。
- `case.go`：业务流程或案例记录。

## 维护建议

- 对外接口不要直接暴露模型结构，优先由 handler 或 service 组织响应字段。
- 状态字段变更后，同步检查 WebSocket 消息处理和测试用例。
